//Create a function that will add two positive numbers of indefinite size. 
//The numbers are received as strings and the result 
//should be also provided as string.

const convertNumber = (num1,num2) => {
    return console.log(`${num1.toString()}`,`${num2.toString()}`)
}
convertNumber(5,8)